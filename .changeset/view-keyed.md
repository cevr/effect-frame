---
"effect-frame": minor
---

Add `View.keyed(source, keyBy, row)`, a keyed region with one row. The row's setup runs again only when the key changes. It replaces a `View.list` over a one-item array.
