# Effect Frame: acceptance matrix

Date: 2026-09-18. This table lists the claims Effect Frame makes about its destination, and the test that proves each one today. It is filled from the tests that exist in this repository right now. A claim with no test is Open, and the row says what is missing.

Read "Proven" as: a test in `bun run gate` fails if the claim stops being true. Read "Proven in-memory only" as: the claim holds over an in-process store or transport, and no test drives the real durable host. Read "Open" as: no automated proof exists yet.

## The matrix

| Claim                                                            | Proof                                                                                                                                                                                                                             | Status                                                                                                                  |
| ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| One actor interface for a local actor                            | `packages/actor/tests/local.test.ts` — "simple state replaces its value through Set", "messages apply in admission order"                                                                                                         | Proven                                                                                                                  |
| One actor interface for a durable actor                          | `packages/actor/tests/durable.test.ts` — "call commits the command and returns the applied revision"                                                                                                                              | Proven                                                                                                                  |
| One actor interface for a remote actor                           | `packages/actor/tests/remote.test.ts` — "call applies through the transport and updates the snapshot"                                                                                                                             | Proven                                                                                                                  |
| Placement is visible in the reference type                       | `packages/actor/tests/types.test.ts` — "placement is visible in the type"                                                                                                                                                         | Proven                                                                                                                  |
| Simple state and machines share one reference                    | `packages/actor/tests/local.test.ts` — "a machine is one kind of behavior behind the same reference", "a machine's own transition reaches the state source"                                                                       | Proven                                                                                                                  |
| The snapshot hides private state                                 | `packages/actor/tests/remote.test.ts` — "the snapshot hides private state"                                                                                                                                                        | Proven                                                                                                                  |
| A client bundle carries no server code                           | `packages/actor/tests/boundary.test.ts` — "the client entry bundles without stores, hosts, or implementations"; `apps/notes/tests/e2e.test.tsx` — "the server renders the page, the bundle, and the resume payload"               | Proven                                                                                                                  |
| Browser JSX draws and updates                                    | `packages/view/tests/dom.test.tsx` — "a bound source writes its first value and then every change", "a keyed list adds, removes, and reorders its rows"                                                                           | Proven                                                                                                                  |
| OpenTUI JSX draws and updates                                    | `packages/view/tests/opentui.test.tsx` — "the same view draws a bound source in a terminal", "typing into an input sends every keystroke to the draft actor"                                                                      | Proven                                                                                                                  |
| One contract feeds a browser page and a terminal at once         | `apps/notes/tests/e2e.test.tsx` — "the terminal sees the same actor at the same revision"                                                                                                                                         | Proven                                                                                                                  |
| Server render escapes every value it writes                      | `packages/view/tests/ssr.test.tsx` — "the server renders the snapshot and escapes everything it writes"                                                                                                                           | Proven                                                                                                                  |
| A render owns and releases its own resources                     | `packages/view/tests/ssr.test.tsx` — "a render releases its own resources before the string returns", "concurrent renders never share state"                                                                                      | Proven                                                                                                                  |
| Hydration adopts the server nodes with no mismatch               | `packages/view/tests/ssr.test.tsx` — "the client adopts the server nodes, agrees with them, and then follows changes"; `apps/notes/tests/e2e.test.tsx` — "the browser hydrates the server page and follows a second client"       | Proven                                                                                                                  |
| Hydration reports a disagreement and the client wins             | `packages/view/tests/ssr.test.tsx` — "a disagreeing server tree is reported and the client wins"                                                                                                                                  | Proven                                                                                                                  |
| Revision handoff loses no revision between render and hydrate    | `packages/view/tests/ssr.test.tsx` — "a revision committed between render and hydrate is applied, not lost"; `packages/actor/tests/remote.test.ts` — "a reference resumes from a held snapshot and receives only later revisions" | Proven                                                                                                                  |
| Reconnect resumes from the last revision seen                    | `packages/actor/tests/http.test.ts` — "a dropped connection reconnects from the last revision"; `apps/notes/tests/e2e.test.tsx` — "a restarted server keeps the actors and the page follows again"                                | Proven                                                                                                                  |
| A retried command ID applies once                                | `packages/actor/tests/durable.test.ts` — "the same command ID with the same payload applies once"; `packages/actor/tests/remote.test.ts` — "a retried command ID applies once across references"                                  | Proven                                                                                                                  |
| The same command ID with a different payload is a conflict       | `packages/actor/tests/durable.test.ts` — "the same command ID with a different payload is a conflict"                                                                                                                             | Proven                                                                                                                  |
| A timed-out call is Uncertain and the command still commits      | `packages/actor/tests/durable.test.ts` — "a timed-out call is Uncertain and the command still commits"                                                                                                                            | Proven                                                                                                                  |
| Typed failures survive the wire with their status                | `packages/actor/tests/http.test.ts` — "typed failures survive the wire with their status"                                                                                                                                         | Proven                                                                                                                  |
| The authorizer can refuse another tenant                         | `packages/actor/tests/remote.test.ts` — "the authorizer sees the key and can refuse another tenant"                                                                                                                               | Superseded by "A policy can refuse another tenant" below                                                                |
| Mailbox conformance: the memory store                            | `packages/actor/tests/memory-store.test.ts` — "MailboxStore conformance: memory > every case passes"                                                                                                                              | Proven                                                                                                                  |
| Mailbox conformance: the Durable Object storage store            | `packages/host-celld/tests/storage-store.test.ts` — "MailboxStore conformance: durable-object storage over bun:sqlite > every case passes"                                                                                        | Proven over bun:sqlite                                                                                                  |
| The store arms an alarm inside the admission transaction         | `packages/host-celld/tests/storage-store.test.ts` — "append arms an alarm inside the admission transaction"                                                                                                                       | Proven                                                                                                                  |
| A reopened store sees the pending command                        | `packages/host-celld/tests/storage-store.test.ts` — "a reopened store over the same storage sees the pending command"                                                                                                             | Proven                                                                                                                  |
| A failed commit transaction leaves no partial state              | `packages/host-celld/tests/storage-store.test.ts` — "a failed commit transaction leaves no partial state"                                                                                                                         | Proven                                                                                                                  |
| celld recovers an accepted command after SIGKILL                 | `packages/host-celld/scripts/crash-harness.ts` row a — "after SIGKILL and restart the alarm drains the command with no request"                                                                                                   | Proven, not in the gate                                                                                                 |
| celld recovers committed state after SIGKILL                     | `packages/host-celld/scripts/crash-harness.ts` row b — "the committed state is unchanged after the retry"                                                                                                                         | Proven, not in the gate                                                                                                 |
| celld returns the stored receipt on a retry                      | `packages/host-celld/scripts/crash-harness.ts` row b — "the retried command returns the stored receipt and does not apply twice"                                                                                                  | Proven, not in the gate                                                                                                 |
| celld rejects a conflicting command ID                           | `packages/host-celld/scripts/crash-harness.ts` row c — "the same command ID with a different payload is a CommandConflict"                                                                                                        | Proven, not in the gate                                                                                                 |
| celld keeps admission order under load                           | `packages/host-celld/scripts/crash-harness.ts` row d — "all three commands applied once and the mailbox drained"                                                                                                                  | Proven, not in the gate                                                                                                 |
| celld recovers interrupted machine work                          | `packages/host-celld/scripts/contract-proof.ts` row g — "machine work resumed after the restart and committed Done one revision on", "Done arrived one full task run after the wake, so the work re-ran"                          | Proven, not in the gate                                                                                                 |
| celld serves the generic contract wire and survives SIGKILL      | `packages/host-celld/scripts/contract-proof.ts` rows e, f, h — committed state restored, retried ID answered from the receipt, conflict typed over the wire, revision event stream                                                | Proven, not in the gate                                                                                                 |
| A durable actor recovers interrupted machine work                | `packages/actor/tests/durable.test.ts` — "machine work interrupted by a restart resumes and commits its own transition"                                                                                                           | Proven in-memory only                                                                                                   |
| Placement is one declaration change, with no consumer change     | none                                                                                                                                                                                                                              | Open: the example serves one host. `NOTES_UPSTREAM` switches the transport, but no test runs the example against celld. |
| The build rejects a server module reachable from a browser entry | `packages/actor/tests/boundary.test.ts` proves the bundle is clean; nothing fails a build that adds a server import                                                                                                               | Open: the proof is a test, not a build rule.                                                                            |

## The progressive layer

The rows below are specified by the map #11 tickets; every row is Open until the build map lands code.

### Define the router, declared route data, and the Transition lifecycle ([#18](https://github.com/cevr/effect-frame/issues/18))

| Claim                                                              | Proof                                                                                                                                                                                                                                                                                                                                                                                                                                            | Status        |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- |
| A route prints what it parses                                      | Property test over every route: `parse(route.href(params, search))` returns the same params and search; `apps/notes/tests/routes.test.ts` — property: `parse(route.href(p, s))` returns `p` and `s` over the whole tree                                                                                                                                                                                                                          | Open          |
| A template that cannot print is refused at definition              | A regexp group and a bare `*` each throw where the route is written; `apps/notes/tests/routes.test.ts` — two fixture definitions each throw at definition time                                                                                                                                                                                                                                                                                   | Open          |
| A search param reorder and an absent optional key both still match | One route matched against `?a=1&b=2`, `?b=2&a=1`, and `?a=1`                                                                                                                                                                                                                                                                                                                                                                                     | Open          |
| Nested patterns compose and a tail forbids children                | A three-deep branch matches one URL and yields every ancestor's params; a parent ending in `:rest*` with children throws                                                                                                                                                                                                                                                                                                                         | Open          |
| A layout's view wraps its child's at the outlet                    | A mounted two-deep branch puts the child's nodes where the layout placed the outlet                                                                                                                                                                                                                                                                                                                                                              | Open          |
| A route resolves its declared data before render on the server     | An SSR render of a branch emits the query's value, with no client round trip                                                                                                                                                                                                                                                                                                                                                                     | Open          |
| Declared data is resolved concurrently on the client               | Two slow declarations resolve in about the time of one; `apps/dashboard/tests/query.test.tsx` — measured wall time under an unbounded-concurrency assertion                                                                                                                                                                                                                                                                                      | Open          |
| A layout's declaration is inherited and resolved once              | A three-deep branch declaring one layout query opens one cache entry; `apps/dashboard/tests/query.test.tsx` — a three-deep branch issues one `TenantInfo` read                                                                                                                                                                                                                                                                                   | Open          |
| A param change keeps the view and refetches only the changed keys  | `packages/router/tests/router.test.tsx` — "a navigation within the same route stays: the view keeps its identity"; the refetch half waits on declared route data                                                                                                                                                                                                                                                                                 | Partly proven |
| An exited segment releases its view scope and its unshared keys    | Navigating away disposes the scope and drops only the keys no surviving segment declares; `apps/notes/tests/query.test.tsx` — `/lists/inbox` to `/scratch` drops `ListCounts`; `/lists/inbox` to `/lists`, the shell's key survives; `apps/dashboard/tests/transition.test.tsx` — the leaf exits, `TenantInfo` stays; the layout exits, it goes; A layout and leaf declaring one key: the leaf exits, the entry stays; the layout exits, it goes | Open          |
| A route's rendering mode is a constructor, not a field             | `prerender` without `inputs` does not compile; no route value carries a mode field                                                                                                                                                                                                                                                                                                                                                               | Open          |
| Prerender renders every input through the SSR pipeline             | A route with three inputs emits three documents at the URLs `href` prints                                                                                                                                                                                                                                                                                                                                                                        | Open          |
| A modified or cross-origin link click is left to the browser       | `packages/router/tests/router.test.tsx` — "a same-origin link click is intercepted; a modified click is not"; the external-origin case is a `followable` branch with no test yet                                                                                                                                                                                                                                                                 | Partly proven |
| A back navigation restores the branch from the URL alone           | `packages/router/tests/router.test.tsx` — "a pop shows the popped URL and reports it as a pop"; the router holds only the shown route's scope                                                                                                                                                                                                                                                                                                    | Proven        |

### Define search params as URL state on the route Schema ([#51](https://github.com/cevr/effect-frame/issues/51))

The URL is the canonical search state. The route Schema owns its decoded vocabulary and its wire spelling.

| Claim                                                                                                            | Proof                                                                                                                                                                                 | Status |
| ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| An omitted default decodes to its value and an equal scalar or array default is omitted                          | `packages/effect-frame/tests/router/route.test.tsx` — "fills omitted defaults and omits equal scalar and array defaults"                                                              | Proven |
| A remapped key and repeated values print in stable Schema order                                                  | `packages/effect-frame/tests/router/route.test.tsx` — "remaps keys, preserves repeated values, and prints in schema order"                                                            | Proven |
| A typed array distinguishes omission, `[]`, and `[""]`, including marker collisions                              | `packages/effect-frame/tests/router/route.test.tsx` — the defaulted array prints no key, `~`, and an empty value; `~`, `~~`, and `~~~` round-trip as user values                      | Proven |
| String literals and all-string unions are valid search fields; dynamic records and non-string arrays are refused | `packages/effect-frame/tests/router/route.test.tsx` — the literal route prints and decodes; type checks reject numeric arrays and tuples, while Boolean and Record schemas throw      | Proven |
| A custom SearchRecord codec keeps its own encoded key order                                                      | `packages/effect-frame/tests/router/route.test.tsx` — the two-pane workspace prints `q`, `filters`, `q2`, `filters2` in the codec's order                                             | Proven |
| A route view can print and update its own URL without importing its route                                        | `packages/effect-frame/tests/router/router.test.tsx` — the Book view uses `RouteProps.href`, `updateSearch`, and `replaceSearch`; the view keeps its node identity on a search update | Proven |
| Concurrent functional updates read the latest URL and keep both changes                                          | `packages/effect-frame/tests/router/router.test.tsx` — "concurrent functional updates serialize against the latest URL"                                                               | Proven |
| Push and replace are separate operations with exact history behavior                                             | `packages/effect-frame/tests/router/router.test.tsx` — push updates, `replaceSearch`, direct `replace`, and a replacing `<Link>` assert the recorded history                          | Proven |
| Retained keys cross routes, caller values override, and an explicit default clears                               | `packages/effect-frame/tests/router/route.test.tsx` — "retains decoded keys across routes and lets explicit values clear defaults"                                                    | Proven |
| Retention survives an invalid unrelated current field and a caller default that encodes away                     | `packages/effect-frame/tests/router/route.test.tsx` — `tenant=acme&page=oops` retains `tenant` while caller `page: 1` is omitted                                                      | Proven |
| A typed updater Link has the same href and activation result as its queued navigation                            | `packages/effect-frame/tests/router/router.test.tsx` — "a typed Link updater prints the same href it navigates"                                                                       | Proven |
| Modified and middle clicks keep native anchor behavior                                                           | `packages/effect-frame/tests/router/router.test.tsx` — the typed anchor receives control and middle clicks without `preventDefault`                                                   | Proven |
| A queued update from a disposed route does not restore that route                                                | `packages/effect-frame/tests/router/router.test.tsx` — "an update from a disposed route does not restore its old URL"                                                                 | Proven |
| A disposed route action cannot mutate a later reentered instance                                                 | `packages/effect-frame/tests/router/router.test.tsx` — saved push and replace actions are no-ops after `/book` exits and reenters                                                     | Proven |
| Rate limiting stays in Source combinators                                                                        | `packages/effect-frame/tests/actor/source.test.ts` — #57 proves debounce and throttle Source combinators; #51 keeps timing out of the router                                          | Proven |

### Define the client-visible command lifecycle and optimistic revisions ([#19](https://github.com/cevr/effect-frame/issues/19))

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                         | Proof                                                                                                                                                                                                                                                                                                                                                                                      | Status |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| `send` returns a handle before the reply lands                | `CommandState` source reads `Sent` with the transport held open; no await on the HTTP reply; `apps/notes/tests/command.test.tsx` — transport held open; `handle.state` is `Sent`, the row is on screen                                                                                                                                                                                     | Open   |
| Admission is observable separately from application           | a handle reaches `Admitted` from the `/send` reply while `committed` is `None`, and `Applied` only from `changes`                                                                                                                                                                                                                                                                          | Open   |
| A provisional revision cannot be compared to a committed one  | type test: `Revision` has no numeric field on the `Provisional` case; `changes(after)` rejects it                                                                                                                                                                                                                                                                                          | Open   |
| An optimistic send shows the new state in the same turn       | a transparent `reducer` actor's `state` source holds the predicted value before the transport resolves; `apps/notes/tests/command.test.tsx` — the predicted row is present before the reply; `"reject-me"` leaves the list as it was                                                                                                                                                       | Open   |
| A committed revision replaces a provisional one               | commit carrying a server-generated field replaces the prediction; no merge                                                                                                                                                                                                                                                                                                                 | Open   |
| A rejected command rolls back by leaving the pending log      | two in-flight commands, the first rejected; the second's state is replayed from the committed base                                                                                                                                                                                                                                                                                         | Open   |
| Provisional order converges on committed order                | two commands committed in the reverse of send order; the final state matches the server's                                                                                                                                                                                                                                                                                                  | Open   |
| A machine behavior is never applied optimistically            | type test: `Behavior.machine` has no `predict`; a machine send reaches `Admitted{provisional: None}`; `apps/auth/tests/command.test.ts` — `SignIn` reaches `Admitted{provisional: None}`; type test: `machine` has no `predict`; `apps/jobs/tests/command.test.tsx` — `Start` reaches `Admitted{provisional: None}`                                                                        | Open   |
| Uncertain keeps the provisional state and retries the same id | a dropped reply, then a retry that hits the stored receipt; the actor applied once; drop the reply to a committed command, re-send the same id, assert the handle reaches `Applied` at the receipt's revision and the actor applied once; `apps/jobs/tests/command.test.tsx` — a dropped reply; the retry hits the stored receipt; at attempt 8 the state is still `Uncertain{attempt: 8}` | Open   |
| Uncertain never becomes Rejected by exhausting retries        | after the attempt bound the state is `Uncertain{attempt: 8}`, not `Rejected`                                                                                                                                                                                                                                                                                                               | Open   |
| A dependent query stays stale while any command is unsettled  | two overlapping commands; the first's `refreshed` does not clear `stale`; `apps/dashboard/tests/query.test.tsx` — two overlapping `Fulfil`s; the first's `refreshed` does not clear `stale`                                                                                                                                                                                                | Open   |
| An override is dropped by any authoritative value             | `override` then an unrelated `refresh`; the overridden value is gone; `apps/dashboard/tests/query.test.tsx` — `override` then an unrelated `refresh`; then a rejected `Ack` leaves the override in place                                                                                                                                                                                   | Open   |

### Define authorization for queries and actors under a required Authorizer ([#20](https://github.com/cevr/effect-frame/issues/20))

| Claim                                                  | Proof                                                                                                                                                                                                                                                                       | Status |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| A host with an unresolvable policy name fails to build | `packages/actor/tests/policy.test.ts` — "a host whose table lacks a named policy fails to build", "the failure names every missing policy, not the first"; `apps/auth/tests/policy.test.ts` — the fixture missing `"admin"` fails `ActorHost.layer` with both misses listed | Open   |
| Allow-all exists only when it is named                 | `packages/actor/tests/policy.test.ts` — "allow-all must be named"; `apps/auth/tests/policy.test.ts` — removing `"public"` refuses the login route's data; no implicit grant anywhere                                                                                        | Open   |
| A policy can refuse another tenant                     | `packages/actor/tests/remote.test.ts` — "the policy sees the principal and can refuse another tenant"                                                                                                                                                                       | Open   |
| An anonymous caller is refused over the wire           | `packages/actor/tests/http.test.ts` — "an anonymous request to a protected actor is 403"; `apps/auth/tests/http.test.ts` — `POST /actors/send` with no cookie answers 403 `Unauthorized`                                                                                    | Open   |
| One policy guards an actor and a query alike           | `packages/actor/tests/query.test.ts` — "one policy name refuses the same tenant through both a command and a query"; `apps/auth/tests/policy.test.ts` — a member calling `Members` and sending to another `Session` is refused by one rule                                  | Open   |
| A client bundle carries no policy table                | `packages/actor/tests/boundary.test.ts` — "the client entry carries the principal type but no policy table"; `apps/auth/tests/boundary.test.ts` — the browser entry bundles `Principal` and no `Policies`                                                                   | Open   |

### Define plain-form command submission without JavaScript ([#21](https://github.com/cevr/effect-frame/issues/21))

| Claim                                                                     | Proof                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Status                        |
| ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- |
| A form with no JavaScript submits a command                               | `apps/notes/tests/plain-form.test.ts` — "a urlencoded post to /actors/form applies the message and answers 303"; `apps/wizard/tests/plain-form.test.ts` — a urlencoded post to `/actors/form` applies `SetAbout` and answers 303 to the next step                                                                                                                                                                                                                                                               | Open                          |
| The server renders the action, the method, and the hidden command id      | `packages/view/tests/ssr.test.tsx` — "a submit binding renders method, action, and the four framework fields"; `apps/wizard/tests/render.test.tsx` — every step's form carries them, hidden inputs first                                                                                                                                                                                                                                                                                                        | Open                          |
| One message schema decodes from JSON and from form fields                 | `packages/actor/tests/form-codec.test.ts` — "the same contract decodes a nested key and message from a flat field map", type test: both codecs yield one type; `apps/wizard/tests/form-codec.test.ts` — `SetAbout` decodes from a flat field map and from JSON to one type; type test asserts the equality                                                                                                                                                                                                      | Open                          |
| An unchecked checkbox decodes as false                                    | `packages/actor/tests/form-codec.test.ts` — "an absent boolean field takes the schema's decoding default"; type test: a form-bound boolean without a default fails to build; `apps/wizard/tests/form-codec.test.ts` — `subscribe` absent is `false`; type test on the fixture without the default                                                                                                                                                                                                               | Open                          |
| A double post of one rendered form applies once                           | `apps/notes/tests/plain-form.test.ts` — "the identical body posted twice returns the stored receipt and one revision"; the same rendered form posted twice applies once; `apps/wizard/tests/plain-form.test.ts` — the identical body posted twice yields one application and one `Duplicate`; `apps/jobs/tests/plain-form.test.ts` — the identical body twice; one job id, one `Duplicate`; `apps/notes/tests/plain-form.test.ts` — "the same rendered Add form posted twice yields one note and one Duplicate" | Open                          |
| A refused post re-renders with issues and the typed values                | `apps/notes/tests/plain-form.test.ts` — "a bad value answers 200 with the page, the issues, and the submitted fields"; `apps/wizard/tests/plain-form.test.ts` — a bad `email` re-renders `AboutStep` with `FormIssues` and the submitted `name`                                                                                                                                                                                                                                                                 | Open                          |
| A re-rendered form after a failure carries a fresh id                     | `apps/notes/tests/plain-form.test.ts` — "the corrected resubmission applies and does not raise CommandConflict"; the corrected resubmission applies and does not raise `CommandConflict`                                                                                                                                                                                                                                                                                                                        | Open (was Open #21 under #19) |
| A field whose segment starts with `_` never re-renders                    | `packages/view/tests/ssr.test.tsx` — "a redacted field is absent from the repopulated form"; `apps/wizard/tests/plain-form.test.ts` — `_notes` is absent from the re-rendered form                                                                                                                                                                                                                                                                                                                              | Open                          |
| A return URL that leaves the origin is refused before the command is sent | `apps/notes/tests/plain-form.test.ts` — "a protocol-relative $return answers 400 and the actor is untouched"; `apps/wizard/tests/plain-form.test.ts` — a protocol-relative `$return` answers 400 and the actor is untouched                                                                                                                                                                                                                                                                                     | Open                          |
| Hydration adopts the rendered command id                                  | `apps/notes/tests/e2e.test.tsx` — "a post that races hydration applies once"; the client's first send carries the id from the markup; `apps/wizard/tests/hydrate.test.tsx` — a post racing the bundle applies once; the client's first send carries both values from the markup                                                                                                                                                                                                                                 | Open                          |
| A multipart post is refused, not mis-parsed                               | `apps/notes/tests/plain-form.test.ts` — "a multipart body answers 415"; `apps/wizard/tests/plain-form.test.ts` — 415 naming the refusal; no partial decode                                                                                                                                                                                                                                                                                                                                                      | Open                          |

### Define streaming SSR, serialized pending queries, and hydration order ([#22](https://github.com/cevr/effect-frame/issues/22))

| Claim                                                                               | Proof                                                                                                                                                                                                                                                                                                       | Status |
| ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| The shell and its fallbacks reach the client before a pending query settles         | a test whose query never settles asserts the first chunk contains the fallback markup and the placeholder record, and that the response is still open; `apps/notes/tests/streaming.test.tsx` — `ListCounts` held open; the first chunk holds the skeleton and the `Placeholder`, the response is still open | Open   |
| A placeholder always precedes its patch                                             | a test with a query that settles during the shell render asserts the placeholder record's document position is before its patch's; `apps/dashboard/tests/streaming.test.tsx` — document order asserted per id; `Closed` lists all eight                                                                     | Open   |
| A record id is identical on both sides                                              | render a page server-side and mount the same view client-side; assert every placeholder id matches a cache entry's derived id                                                                                                                                                                               | Open   |
| A patch that arrives before hydration replaces the fallback, and the report says so | settle the query before `hydrate`; assert the content is in the tree and `resolvedAhead` is 1 with no unexplained mismatch; `apps/notes/tests/streaming.test.tsx` — settle before `hydrate`; `resolvedAhead` is 1, no unexplained mismatch                                                                  | Open   |
| A patch that arrives after hydration settles the scope with no claim                | hydrate against a fallback, append a patch record, assert the content appears and `unclaimed` is 0                                                                                                                                                                                                          | Open   |
| A late patch is observed with no script execution                                   | assert the document contains no `<script>` without `type="application/json"` besides the module tag, and that a record appended after hydrate still lands                                                                                                                                                   | Open   |
| A query still pending when the stream ends becomes `Failed{StreamEnded}`            | end the stream with one query unsettled; assert the entry is `Failed{StreamEnded}` and the scope shows no fallback; `apps/notes/tests/streaming.test.tsx` — truncate before `Closed`; the entry fails, no scope hangs, then `/query` reaches `Ready`                                                        | Open   |
| A `StreamEnded` entry converges over the ordinary query path                        | the same test asserts the client posts `/query` for that key and reaches `Ready`                                                                                                                                                                                                                            | Open   |
| A truncated response reaches the same verdict as a closed one                       | drop the response before `Closed`; assert the same `Failed{StreamEnded}`                                                                                                                                                                                                                                    | Open   |
| An `AwaitAll` route writes no record channel and hydrates with no mismatch          | assert the document has no `#frame-records` and the report is empty; `apps/notes/tests/streaming.test.tsx` — `/lists/inbox/print` has no `#frame-records`, and `HydrationReport` is empty                                                                                                                   | Open   |
| A duplicate patch for a settled id changes nothing                                  | append a patch twice; assert one settle and an unchanged value                                                                                                                                                                                                                                              | Open   |
| The op wire starts only after the document closes                                   | a server-driven view with a streamed query; assert no op is applied before `Closed` and hydration finish; `apps/chat/tests/driven.test.tsx` — no op is applied before `Closed` and the hydration report                                                                                                     | Open   |

### Define prerender inputs, build pipeline, and resume ([#23](https://github.com/cevr/effect-frame/issues/23))

| Claim                                                                                            | Proof                                                                                                                                                                                                                                                                                                   | Status |
| ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| A prerendered file's path is printed by the same `href` every link uses                          | for each built page, assert `manifest.json`'s `href` equals `route.href(input)` and the file sits at `dist/prerender/<href>/index.html`; `apps/blog/tests/build.test.ts` — for each page, `manifest.json`'s href equals `PostRoute.href(input)` and the file sits at `dist/prerender/<href>/index.html` | Open   |
| Every link a prerendered page renders points at a page that was built                            | scan the rendered index for `href` attributes and assert each one is in the manifest; `apps/blog/tests/build.test.ts` — scan `IndexView`'s output for `href` and assert each is in the manifest                                                                                                         | Open   |
| Nested prerender routes enumerate the product of parent and child inputs                         | a two-level fixture with 3 parents and 2 children each builds 6 pages, and the child's `inputs` Effect runs 3 times                                                                                                                                                                                     | Open   |
| A prerender leaf under a param-contributing non-prerender ancestor is refused at definition time | constructing the tree fails with `PrerenderAncestorNotEnumerable` naming the ancestor and the param; `apps/blog/tests/build.test.ts` — the fixture tree throws `PrerenderAncestorNotEnumerable` naming the ancestor and `org`                                                                           | Open   |
| A prerender leaf under a plain layout that contributes no params is allowed                      | the same fixture with a param-free layout constructs and builds                                                                                                                                                                                                                                         | Open   |
| A prerendered document is `AwaitAll`: no placeholders, no `Closed`                               | assert the document has no `Placeholder` and no `Closed` record, and that every declared query has a `Patch`; `apps/blog/tests/document.test.ts` — record channel holds only `Patch` records                                                                                                            | Open   |
| A prerendered render that a policy refuses for `Anonymous` fails the build                       | a route whose query names a policy refusing `Anonymous`; assert the build exits non-zero with `PrerenderUnauthorized` and writes no file; `apps/blog/tests/build.test.ts` — the `Draft` fixture route exits non-zero with `PrerenderUnauthorized`; `dist/prerender` is untouched                        | Open   |
| The build issues one read for a query two routes share                                           | a fixture whose index query also feeds `inputs`; assert one handler invocation for N+1 pages; `apps/blog/tests/build.test.ts` — one `PostIndex` handler invocation for N+1 pages                                                                                                                        | Open   |
| A rebuild produces the same tree                                                                 | run the build twice over an unchanged store; assert the trees are byte-identical and the manifest differs only in `builtAt`; `apps/blog/tests/build.test.ts` — two builds diffed                                                                                                                        | Open   |
| A crashed build leaves the previous tree serving                                                 | abort mid-build; assert `dist/prerender` still holds the previous manifest and pages; `apps/blog/tests/build.test.ts` — abort mid-build; the previous manifest and pages are intact                                                                                                                     | Open   |
| A prerendered page's actor resumes from the baked revision and catches up                        | build at revision R, commit after the build, mount; assert the island shows the later state and `changes` was called with `after: R`; `apps/blog/tests/island.test.tsx` — build, `Heart` after the build, mount; the island shows the later count and `changes` was called with `after: R`              | Open   |
| A client whose baked revision the store no longer holds takes the newest                         | build at R, reset the actor's store, mount; assert the island converges on the store's latest and no scope hangs; `apps/blog/tests/island.test.tsx` — reset the store, mount; the island converges, no scope hangs                                                                                      | Open   |
| A baked query value paints immediately and is marked stale                                       | mount a prerendered document; assert no readiness fallback is drawn, the entry is `Ready{stale: true}`, and the value is the baked one; `apps/blog/tests/document.test.ts` — no skeleton is drawn; the entry is stale; one read per baked key reaches `Ready{stale: false}`                             | Open   |
| A baked query revalidates on load and confirms                                                   | the same test asserts one read is issued per baked key and the entry reaches `Ready{stale: false}`                                                                                                                                                                                                      | Open   |
| A prerender route with no file on disk answers through SSR                                       | delete the file, request the route; assert a 200 with the same document and query patches carrying no `builtAt`; `apps/blog/tests/serve.test.ts` — delete one file, request it; 200, same document, `Patch` carries no `builtAt`                                                                        | Open   |
| A prerendered file is served before the router runs                                              | instrument the router; assert a hit answers with zero route matches; `apps/blog/tests/serve.test.ts` — zero route matches on a hit; `If-None-Match` answers 304                                                                                                                                         | Open   |
| A prerendered page revalidates with `ETag` and answers 304                                       | request with `If-None-Match` from the manifest; assert 304 and an empty body                                                                                                                                                                                                                            | Open   |
| A prerendered page with a client island loads `/client.js` and hydrates                          | assert the module tag is present and `HydrationReport` is empty                                                                                                                                                                                                                                         | Open   |

### Define the server/client file convention and its build rule ([#24](https://github.com/cevr/effect-frame/issues/24))

This ticket restates one row of the matrix above rather than adding a new claim. It replaces that row's proof and status once `tooling/checks` lands.

| Claim                                                            | Proof                                                                                                                                                                                                                                                                                                                                                                                          | Status |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| The build rejects a server module reachable from a browser entry | `tooling/checks/tests/boundary.test.ts` — "a server module three files deep is refused with its path chain", "the full actor entry is refused from a browser entry"; `bun run boundary` in `bun run gate` fails the build and names the import chain; `apps/notes/tests/boundary.test.ts` — an injected `import "./queries.server.js"` in `page.tsx` fails `bun run build` with the path chain | Open   |

### Specify the seven example applications and their acceptance rows ([#25](https://github.com/cevr/effect-frame/issues/25))

Each example carries its own rows. The deciding ticket sits inside the Claim cell, so the table keeps three columns.

#### Notes — every rendering mode of one page

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                                                                                                                                                                        | Proof                                                                                                                                                                                                                                                                                                                              | Status                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| One route tree serves SSR, streaming, `AwaitAll` and SPA with no per-mode branch in a view ([#18](https://github.com/cevr/effect-frame/issues/18), [#22](https://github.com/cevr/effect-frame/issues/22))    | `apps/notes/tests/modes.test.tsx` — "the same `ListView` renders under `ssr`, `streaming`, `AwaitAll` and `client`"; assert the view module contains no mode check                                                                                                                                                                 | Open                             |
| One command refreshes both dependent queries in one reply, and none when neither is on screen ([#17](https://github.com/cevr/effect-frame/issues/17), [#28](https://github.com/cevr/effect-frame/issues/28)) | `apps/notes/tests/query.test.tsx` — `Add` from `/lists/inbox` answers `refreshed` for two keys; the same `Add` from `/scratch` answers zero; `apps/dashboard/tests/single-flight.test.tsx` — `Fulfil` from overview refreshes 3; `Ack` refreshes 1; `Fulfil` from orders refreshes 2; a command on an undepended actor refreshes 0 | Open                             |
| `Loading` outside `Errored` and `Errored` outside `Loading` both show exactly one fallback ([#16](https://github.com/cevr/effect-frame/issues/16), [#26](https://github.com/cevr/effect-frame/issues/26))    | `packages/view/tests/readiness.test.tsx` — "a failure routes to the Errored scope and the loading fallback lets go" (Errored outside Loading, with the failure bound and a retry); the other nesting order is untested                                                                                                             | Partly proven                    |
| A refetch holds stale content and never draws the fallback twice ([#17](https://github.com/cevr/effect-frame/issues/17))                                                                                     | `packages/view/tests/readiness.test.tsx` — "a refetch holds the content and exposes the stale flag"; `packages/actor/tests/query.test.ts` — "followQuery keeps the last value, stale, while the next key loads"                                                                                                                    | Proven in packages; app row open |
| One contract still feeds a browser page and a terminal at once                                                                                                                                               | `apps/notes/tests/e2e.test.tsx` — existing row, unchanged                                                                                                                                                                                                                                                                          | Proven                           |

#### Blog — prerender, typed links, one client island

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                                                                                                               | Proof                                                                                                                                         | Status |
| --------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| A generated field drawn from fresh entropy is minted at render and stable across a resubmit ([#32](https://github.com/cevr/effect-frame/issues/32)) | `apps/blog/tests/island.test.tsx` — the `Heart` form's hidden `id` is not equal to `$command` and is unchanged across two posts of one render | Open   |

#### Auth — session actor, protected routes, revocation

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                                                                                    | Proof                                                                                                                       | Status |
| ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- | ------ |
| A route policy is resolved before any declared data ([#20](https://github.com/cevr/effect-frame/issues/20))              | `apps/auth/tests/routes.test.ts` — an anonymous navigation to `/app` issues zero query reads before redirecting             | Open   |
| Route policies accumulate and a child cannot loosen an ancestor ([#20](https://github.com/cevr/effect-frame/issues/20))  | `apps/auth/tests/routes.test.ts` — a member reaching `/app/admin` is refused although `admin` alone would pass for an admin | Open   |
| A public page survives a sign-out by reconnecting as `Anonymous` ([#30](https://github.com/cevr/effect-frame/issues/30)) | `apps/auth/tests/revocation.test.ts` — `/login` keeps following after the stream it shared was closed                       | Open   |

#### Dashboard — many queries, one live stream, single flight at width

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                                                                                                        | Proof                                                                                                                       | Status |
| -------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------ |
| A commit to an actor no query depends on does zero extra work ([#17](https://github.com/cevr/effect-frame/issues/17))                        | `apps/dashboard/tests/single-flight.test.tsx` — the early return is observed; no query handler runs                         | Open   |
| Canonical args make `{a,b}` and `{b,a}` one entry ([#17](https://github.com/cevr/effect-frame/issues/17))                                    | `apps/dashboard/tests/query.test.tsx` — two declarations with reordered arg keys open one entry                             | Open   |
| A failed refresh answers `RefreshFailed` and does not undo the command ([#17](https://github.com/cevr/effect-frame/issues/17))               | `apps/dashboard/tests/single-flight.test.tsx` — a throwing `Funnel` handler; the `Fulfil` receipt still carries `committed` | Open   |
| Invalidate-on-send then accept-on-reply holds stale content for the whole round trip ([#17](https://github.com/cevr/effect-frame/issues/17)) | `apps/dashboard/tests/query.test.tsx` — the revenue figure is on screen and marked stale from send until `refreshed`        | Open   |
| A nested `Loading` holds only its own card while the page paints ([#16](https://github.com/cevr/effect-frame/issues/16))                     | `apps/dashboard/tests/readiness.test.tsx` — the slow card shows a fallback while the other seven show content               | Open   |
| A source registered after first paint flips its scope pending again ([#16](https://github.com/cevr/effect-frame/issues/16))                  | `apps/dashboard/tests/readiness.test.tsx` — the funnel card mounts late; the scope re-enters pending and settles            | Open   |

#### Wizard — a multi-step command flow that completes with JavaScript disabled

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                                                                                    | Proof                                                                                                                         | Status |
| ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | ------ |
| Coercion is declared in the Schema, never in the field name ([#21](https://github.com/cevr/effect-frame/issues/21))      | `apps/wizard/tests/form-codec.test.ts` — `age` decodes from `"34"`; no field name carries a prefix                            | Open   |
| `__proto__` in a field name is refused structurally ([#21](https://github.com/cevr/effect-frame/issues/21))              | `apps/wizard/tests/form-codec.test.ts` — a hostile body is refused before the message schema is consulted                     | Open   |
| A structural failure never reaches `FormIssues` ([#21](https://github.com/cevr/effect-frame/issues/21))                  | `apps/wizard/tests/plain-form.test.ts` — a hostile body cannot steer a 200                                                    | Open   |
| `$version` mismatch is a 409 and is not decoded leniently ([#21](https://github.com/cevr/effect-frame/issues/21))        | `apps/wizard/tests/plain-form.test.ts` — a stale page posting version 1 to a version 2 host is `ContractMismatch`             | Open   |
| The whole flow completes end to end with no client bundle loaded ([#21](https://github.com/cevr/effect-frame/issues/21)) | `apps/wizard/tests/plain-form.test.ts` — five posts and four redirects reach `Submitted`; no request for `/client.js` is made | Open   |

#### Long job — machine progress that survives a restart, on two hosts

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                                                                                                                                    | Proof                                                                                                                                                                                                            | Status                                                                |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| Interrupted machine work resumes after a restart and commits its own transition ([#19](https://github.com/cevr/effect-frame/issues/19))                                  | `apps/jobs/tests/resume.test.ts` — kill mid-task, restart, assert the job reaches `Finished` and the work re-ran once                                                                                            | Open (in-memory row exists in `packages/actor/tests/durable.test.ts`) |
| The same application runs on Bun and on celld with no consumer change ([#27](https://github.com/cevr/effect-frame/issues/27), map row "Hosts")                           | `apps/jobs/tests/hosts.test.ts` — the whole suite parameterised over both layers; a diff of the client sources between runs is empty; `apps/chat/tests/hosts.test.ts` — the suite parameterised over both layers | Open                                                                  |
| A restarted celld node drains the accepted command with no request ([#27](https://github.com/cevr/effect-frame/issues/27))                                               | `apps/jobs/scripts/proof-jobs.ts` — SIGKILL after admission; the alarm drains it                                                                                                                                 | Open, will be "not in the gate"                                       |
| Autonomous progress and command commits share one revision clock ([#19](https://github.com/cevr/effect-frame/issues/19))                                                 | `apps/jobs/tests/resume.test.ts` — an `advance` and a `Cancel` interleave with strictly increasing revisions                                                                                                     | Open                                                                  |
| A page driven by a live actor draws no readiness fallback ([#16](https://github.com/cevr/effect-frame/issues/16), [#18](https://github.com/cevr/effect-frame/issues/18)) | `apps/jobs/tests/render.test.tsx` — `JobView` has no `Loading` and its first paint carries the progress figure                                                                                                   | Open                                                                  |
| A machine with no client attached and no pending command stays dormant until a request or an alarm ([map, gap 1](https://github.com/cevr/effect-frame/issues/11))        | `apps/jobs/tests/resume.test.ts` — asserted as the **current, documented** behaviour, so a heartbeat alarm later turns this row over deliberately                                                                | Open                                                                  |

#### Chat — a server-driven view over a shared durable actor

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                                                                                                                              | Proof                                                                                                                                              | Status |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| A server-driven view draws through the same seven-operation host interface ([#15](https://github.com/cevr/effect-frame/issues/15))                                 | `apps/chat/tests/driven.test.tsx` — `RoomView` mounts against a `RemoteHost` and against a DOM host from one file, with no view vocabulary added   | Open   |
| An eliding recorder sends far less than markup for a small change ([#15](https://github.com/cevr/effect-frame/issues/15))                                          | `apps/chat/tests/driven.test.tsx` — one `Post` into a 20-message room; the encoded patch is an order of magnitude under `renderToString`           | Open   |
| A server view nests a client child, and imports point one way ([#24](https://github.com/cevr/effect-frame/issues/24))                                              | `apps/chat/tests/boundary.test.ts` — `Composer` is in the browser bundle, `room.server.ts` is not, and the build fails when the import is reversed | Open   |
| A recorder's shadow never drifts from a reference DOM host ([#15](https://github.com/cevr/effect-frame/issues/15) risk 4)                                          | `apps/chat/tests/driven.test.tsx` — 200 random messages replayed against a reference host; the trees are equal at every step                       | Open   |
| `Route.driven` forbids a client-only view ([#18](https://github.com/cevr/effect-frame/issues/18))                                                                  | `apps/chat/tests/routes.test.ts` — the fixture fails to construct; type test asserts it does not compile                                           | Open   |
| Presence is a field of the snapshot, with no second store and no clock beside the state ([map, Not yet specified](https://github.com/cevr/effect-frame/issues/11)) | `apps/chat/tests/presence.test.tsx` — a disconnect emits `Leave` and the roster shrinks by one revision; no module holds a presence map            | Open   |

### Decide reconnect for the streamed host-operation wire ([#27](https://github.com/cevr/effect-frame/issues/27))

| Claim                                                                 | Proof                                                                                                                                                                                                                                                             | Status                           |
| --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| A reconnecting server-driven client resumes from the actor's snapshot | a test that drops a client, passes revisions, re-mounts from the resume payload, and compares the tree to `Html.renderToString` at latest; `apps/chat/tests/reconnect.test.tsx` — drop, pass revisions, re-mount; the tree equals `Html.renderToString` at latest | Open                             |
| A reconnect costs the snapshot, not the first-mount op log            | the same test asserts the resume payload stays under the encoded rebuild patch — measured 1015 B against 22489 B at 20 notes; `apps/chat/tests/reconnect.test.tsx` — the resume payload stays well under the encoded rebuild patch                                | Open                             |
| A client at any age reconnects at constant cost                       | drop a client, pass N revisions for several N, assert the reconnect payload does not grow with N; `apps/jobs/tests/reconnect.test.tsx` — drop, pass N revisions for several N; the resume payload does not grow with N                                            | Open                             |
| A stale patch is refused and never silently applied                   | a client holding an old revision is given a later patch and fails `StaleClient`; the tree is unchanged; `apps/chat/tests/reconnect.test.tsx` — a client holding an old revision is given a later patch and fails `StaleClient`; the tree is unchanged             | Open                             |
| The server holds no per-client history                                | after a client disconnects, the server retains no ops and no snapshot for it; `apps/chat/tests/reconnect.test.tsx` — after a disconnect the server retains no ops and no per-client snapshot                                                                      | Open                             |
| A rebuilt server-side mount reproduces the client tree exactly        | mount, reconnect, re-mount, compare to `Html.renderToString` and to a reference DOM host                                                                                                                                                                          | Open                             |
| Rendering is deterministic from a snapshot                            | two fresh mounts at one snapshot produce identical op logs — **verified in the prototype, 63 ops, identical**; `apps/chat/tests/driven.test.tsx` — two fresh mounts at one snapshot produce identical op logs                                                     | Open, promote from the prototype |

### Decide the query cache entry lifetime that makes "active" mean "on screen" ([#28](https://github.com/cevr/effect-frame/issues/28))

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                            | Proof                                                                                                                                                                                                          | Status |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| An unmounted view's query entry is released                      | `packages/actor/tests/query.test.ts` — "an entry is released when its last declaring scope closes", "a released entry's read in flight is interrupted"                                                         | Proven |
| `active` names both branches during a transition and one after   | `active` measured mid-transition and post-transition on a `/posts/1` to `/other` move; `apps/dashboard/tests/transition.test.tsx` — measured mid-transition and after on overview → orders                     | Open   |
| A session that opens and closes N screens declares a bounded set | `active` after 50 navigations is the current branch, not 50 branches; `apps/dashboard/tests/transition.test.tsx` — `active` after 50 moves is the current branch                                               | Open   |
| A server render holds one cache for one request and releases it  | Two concurrent renders share no entry; both caches are gone when the responses end; `apps/dashboard/tests/streaming.test.tsx` — two concurrent renders share no entry; both caches gone when the responses end | Open   |

### Define batched queries for per-row reads ([#54](https://github.com/cevr/effect-frame/issues/54))

| Claim                                                                                    | Proof                                                                                                                                                                                                  | Status |
| ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| A batched declaration is visible in the contract and has a real batch wire verb          | `packages/effect-frame/tests/actor/query.test.ts` — "coalesces one scheduler turn and shares duplicate keys" crosses `POST /actors/query/batch` through the HTTP client, server, and cache             | Proven |
| One scheduler turn sends many keys to one batch resolver invocation                      | `packages/effect-frame/tests/actor/query.test.ts` — the distinct `BatchedLookup` keys share one HTTP request and one `Query.batched` resolver call                                                     | Proven |
| The resolver receives all valid authorized arguments once, with no unauthorized argument | `packages/effect-frame/tests/actor/query.test.ts` — "isolates invalid, stale-version, and unauthorized keys" observes only the valid `id` in the resolver and returns one aligned result per input key | Proven |
| Success and failure stay in separate typed states                                        | `packages/effect-frame/tests/actor/query.test.ts` — "keeps success and per-key failure in one batch" leaves one key `Ready` and the other `Failed{QueryFailed}`                                        | Proven |
| Cancelling one key does not cancel another live key                                      | `packages/effect-frame/tests/actor/query.test.ts` — "cancelling one key leaves another live key in the batch" closes the first scope while the second resolves                                         | Proven |
| `View.list` row setup opens two visible rows in one batch wire request                   | `packages/effect-frame/tests/view/query-batch.test.tsx` — rows peel their first item value, call `useQuery`, and cross `POST /actors/query/batch` once                                                 | Proven |
| Closing every row aborts an in-flight batch and cleans the server request                | `packages/effect-frame/tests/view/query-batch.test.tsx` — both row scopes close, `QueryCache.active` is empty, the HTTP abort reaches the host, and the resolver cleanup runs before the gate opens    | Proven |
| A command refreshes active dependent batched keys through one resolver invocation        | `packages/effect-frame/tests/actor/query.test.ts` — "refreshes all active dependent keys through one batch resolver" accepts both refreshed values after one command reply                             | Proven |

### Decide how a client resolves an Uncertain command from the changes stream ([#29](https://github.com/cevr/effect-frame/issues/29))

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                                        | Proof                                                                                                                                                                                                                                                                                                                                                | Status                             |
| ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| An Uncertain command settles the same way after a reconnect                  | settle a handle whose changes stream was disconnected across the commit and reconnected after it; the settling path is the same re-send, and no revision attribution is read; `apps/jobs/tests/command.test.tsx` — reconnect mid-uncertainty; the handle settles from the re-send's `Duplicate{receipt}`; the transport exposes no receipt-read door | Open                               |
| The changes stream carries no command identity                               | type test: `Projection` and `WireProjection` have `revision` and `snapshot` and no third field                                                                                                                                                                                                                                                       | Open                               |
| The changes stream may skip revisions a client missed                        | a subscriber absent across several commits receives the newest revision only, and its state matches the actor's                                                                                                                                                                                                                                      | Open, documents existing behaviour |
| An autonomous revision is indistinguishable on the wire from a commanded one | `advance` and `commit` produce the same `Projection` shape; no consumer branches on origin                                                                                                                                                                                                                                                           | Open                               |
| A receipt outlives the retry bound                                           | after the #19 attempt bound elapses, `receipt(commandId)` still returns the stored receipt in both stores                                                                                                                                                                                                                                            | Open, add to the conformance suite |
| A store never re-admits a command id it has seen                             | conformance: append, commit, then append the same id again returns `Duplicate` and never `Admitted` — the property any future pruning must preserve                                                                                                                                                                                                  | Open, add to the conformance suite |
| A plain-form resubmission and a scripted retry settle by one mechanism       | the same rendered form posted twice and a scripted re-send of the same id both reach the stored receipt                                                                                                                                                                                                                                              | Open (#21)                         |

### Decide how a live connection learns its principal was revoked ([#30](https://github.com/cevr/effect-frame/issues/30))

| Claim                                                        | Proof                                                                                                                                                                                                                                                                                                        | Status |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| A revoked session closes every open stream it authorized     | `packages/actor/tests/http.test.ts` — "a sign-out on the session actor ends the open changes stream with Unauthorized"; two connections on one session both end from one `SignOut`; `apps/auth/tests/revocation.test.ts` — two `/app/devices` connections on one session both end from one `SignOut`         | Open   |
| A stream ends on any principal change, not only a revocation | same file — "a session that changes subject ends the stream rather than re-authorizing it"; `apps/auth/tests/revocation.test.ts` — a `SignIn` changing `subject` ends the stream rather than re-authorizing it                                                                                               | Open   |
| A session heartbeat does not disturb live connections        | same file — "a Touch that only slides expiresAt publishes a revision and ends no stream"; `apps/auth/tests/revocation.test.ts` — `Touch` publishes a revision; both connections stay open                                                                                                                    | Open   |
| Expiry closes a stream with no per-connection timer          | `packages/actor/tests/durable.test.ts` — "a session machine commits Empty at expiresAt and the watching stream ends"; no timer exists outside the session actor; `apps/auth/tests/revocation.test.ts` — the machine commits `Empty` at `expiresAt`; no timer exists outside the session actor                | Open   |
| N connections on one session cost one subscription           | `packages/actor/tests/http.test.ts` — "ten connections on one session open one hosted session instance"; `apps/auth/tests/revocation.test.ts` — ten connections, one instance behind the host's semaphore; `apps/chat/tests/multi.test.tsx` — three connections, one instance behind the host's semaphore    | Open   |
| A host with no sessions pays nothing for revocation          | `packages/actor/tests/http.test.ts` — "`HttpServer.anonymous` opens no session subscription and never ends a stream"                                                                                                                                                                                         | Open   |
| A revoked stream is not retried                              | `packages/actor/tests/http.test.ts` — "the client fails fast on Unauthorized rather than exhausting the reconnect schedule"; `apps/auth/tests/revocation.test.ts` — the client's reconnect schedule is never entered on `Unauthorized`                                                                       | Open   |
| A revoked navigation lands on login, a refused one does not  | `apps/auth` e2e — anonymous-after-revocation redirects with `next`; authenticated-but-refused renders 403; `apps/auth/tests/routes.test.ts` — anonymous gets `/login?next=/app/admin`; a member gets a rendered 403; `apps/auth/tests/revocation.test.ts` — the two cases end differently from one `SignOut` | Open   |

### Decide scroll restoration and focus management on navigation ([#31](https://github.com/cevr/effect-frame/issues/31))

| Claim                                                                                | Proof                                                                                                                                                                                                                                        | Status |
| ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| A push navigation scrolls to the top when the shell commits, not when the data lands | Navigate to a route with a never-settling query; assert `scrollY` is 0 while the fallback is on screen and the response is still open; `apps/notes/tests/navigation.test.tsx` — three assertions; `history.scrollRestoration` stays `"auto"` | Open   |
| A push navigation to a URL with a fragment scrolls to the fragment, not to the top   | Navigate to `/docs#usage`; assert the element is in view and `scrollY` is not 0                                                                                                                                                              | Open   |
| A back navigation restores the browser's saved position, and the router holds none   | Scroll, navigate away, go back; assert the position is restored and that no router module reads or writes a scroll value or `sessionStorage`                                                                                                 | Open   |
| The router never sets `history.scrollRestoration` to `manual`                        | Assert `history.scrollRestoration` is `"auto"` after mount and after ten navigations                                                                                                                                                         | Open   |
| A late patch fills content in place and does not move the viewport                   | Scroll into a settled region, append a `Patch` for a below-the-fold scope; assert `scrollY` is unchanged                                                                                                                                     | Open   |
| A fragment-only navigation is not intercepted                                        | Click `#section` on the current page; assert no Transition ran and the browser scrolled                                                                                                                                                      | Open   |
| Focus moves to the entering leaf's root on a push navigation                         | Navigate; assert `document.activeElement` is the leaf root and it carries `tabindex="-1"`                                                                                                                                                    | Open   |
| A leaf's own `autofocus` element wins over the leaf root                             | A leaf with `autofocus` on its `<h1>`; assert focus lands on the heading                                                                                                                                                                     | Open   |
| A stayed segment keeps focus across a param or search change                         | Focus a layout's search field, navigate `?q=a` to `?q=b`; assert `activeElement` and the caret position are unchanged                                                                                                                        | Open   |
| A form's failed validation keeps focus in the field                                  | Submit an invalid form; assert no `navigate` event fired and `activeElement` is unchanged                                                                                                                                                    | Open   |
| The router adds no `aria-live` region                                                | Assert the mounted document contains no `aria-live` the app did not write                                                                                                                                                                    | Open   |
| `Preserve` leaves scroll and focus alone                                             | A route with `behavior: Preserve`; assert `scrollY` and `activeElement` are unchanged after navigating to it                                                                                                                                 | Open   |
| Behavior is a value on the leaf, not a flag, and not on a layout                     | `layout(..., { behavior })` does not compile; no route value carries a boolean scroll field                                                                                                                                                  | Open   |
| The fallback path scrolls and focuses without the Navigation API                     | With `navigation` deleted, assert a push scrolls to top and focuses the leaf root, and that `focus` was called with `preventScroll`                                                                                                          | Open   |
| No scroll or focus code is reachable from a server render                            | Assert `renderToStream` of a branch installs no listener and that the server bundle excludes `navigation.ts`                                                                                                                                 | Open   |

### Decide how a plain post supplies a message value no form field carries ([#32](https://github.com/cevr/effect-frame/issues/32))

Rows this ticket restates are listed once under the deciding ticket.

| Claim                                                                                            | Proof                                                                                                                                                                                                                                 | Status |
| ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| A generated field is minted at render and never at decode                                        | `packages/view/tests/ssr.test.tsx` — "a message with a generated field renders it as a hidden input"; `packages/actor/tests/form-codec.test.ts` — "decoding a body missing a generated field fails and mints nothing"                 | Open   |
| A generated field derived from the command id equals it in the markup                            | `packages/view/tests/ssr.test.tsx` — "the hidden `id` and `$command` hold the same value"; `apps/wizard/tests/render.test.tsx` — the hidden `id` and `$command` hold the same value on `/apply`                                       | Open   |
| A re-render after a validation failure mints a new command id and a new generated value together | `apps/notes/tests/plain-form.test.ts` — "the corrected resubmission applies and raises no CommandConflict"; `apps/wizard/tests/plain-form.test.ts` — the corrected resubmission applies and raises no `CommandConflict`               | Open   |
| An `Uncertain` re-render keeps both the command id and the generated value                       | `apps/notes/tests/plain-form.test.ts` — "the 504 re-render posts a byte-identical body and hits the stored receipt"; `apps/wizard/tests/plain-form.test.ts` — a 504 re-render posts a byte-identical body and hits the stored receipt | Open   |
| Hydration adopts a generated value and never re-mints it                                         | `packages/view/tests/ssr.test.tsx` — "the hydrated binding sends the adopted `id`, not a fresh one"                                                                                                                                   | Open   |
| A generated field is absent from the message an author constructs                                | type test: `Add`'s send input omits `id`; `Schema.Schema.Type<typeof Add>` keeps it required                                                                                                                                          | Open   |
| A generated field cannot carry a decoding default                                                | type test: `Schema.withDecodingDefault` over a `Generated` field does not compile                                                                                                                                                     | Open   |
| A form-bound message with an unmarked missing field does not compile                             | type test, extending #21 §1.5's three refusals; type test — the `Uint8Array` fixture and an unmarked generated field each refuse `Form.codec`                                                                                         | Open   |

### Define time-based and Effect-derived sources ([#57](https://github.com/cevr/effect-frame/issues/57))

| Claim                                                                                              | Proof                                                                                                                                                                           | Status |
| -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| A debounced source emits its current value, delays changes, and hides pending values               | `packages/effect-frame/tests/actor/source.test.ts` — "debounce emits the seed and only the latest quiet value"; "does not lose a change made between the read and registration" | Proven |
| A throttled source emits immediately, shapes later values, and closes with its scope               | `packages/effect-frame/tests/actor/source.test.ts` — "throttle emits the first value and shapes later values"; "time-based work ends with its owner scope"                      | Proven |
| An Effect-derived source cancels replaced work and exposes Loading, stale Ready, and Failed states | `packages/effect-frame/tests/actor/source.test.ts` — "starts loading, cancels old work, carries stale data, and fails"                                                          | Proven |

## What "not in the gate" means

`packages/host-celld/scripts/crash-harness.ts` and `scripts/contract-proof.ts` need the celld 0.5.0 binary and take about a minute together. They run with `bun run proof:celld` and `bun run proof:contract` in the `@effect-frame/host-celld` package. Both passed on 2026-09-18 against `main` at `ed076c6`: 10 of 10 and 12 of 12 checks. CI does not run it. Its rows are proven against the real host, but a regression does not turn `bun run gate` red.

## The gaps, in order of risk

1. **Wake after a crash mid-task.** `contract-proof.ts` row g wakes the restarted object with one `snapshot` request, which is what a reconnecting client sends first. An object whose machine is mid-task with no pending command and no client stays dormant until a request or an alarm arrives; a heartbeat alarm for that case is not implemented.
2. **Placement as a declaration.** The example reads `NOTES_UPSTREAM` and swaps one layer. Nothing tests that swap. A test that starts the example against a celld host would close both this row and the one above.
3. **Build enforcement of the import boundary.** A bundle test catches a leak after the fact. A lint rule or a package-export restriction would catch it at the import.
