---
"effect-frame": patch
---

The spans of `runQuery` and `followQuery` are named `Query.run` and `Query.follow`, in the `Area.operation` form every other span has (the new `frame/span-name` lint rule). They were `runQuery` and `followQuery`.
