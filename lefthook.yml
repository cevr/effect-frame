pre-commit:
  parallel: false
  jobs:
    - name: lint+fmt
      run: bun run lint:fix && bun run fmt
      stage_fixed: true
    - name: gate
      run: bun run gate
pre-push:
  jobs:
    - name: format
      run: bun run fmt:check
