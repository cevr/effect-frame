/**
 * The id of the element the Notes page mounts into. The server's document
 * names it (`Html.Document.rootId`) and the browser entry finds it
 * (`Dom.root`), both from here.
 */
export const rootId = "app";
