/**
 * The `effect-frame/view/testing` entry: the `ViewTest` harness. It is a
 * subpath of its own, like `effect-frame/actor/testing`, so a browser entry
 * that imports `effect-frame/view` carries no test harness.
 */
export * as ViewTest from "./testing.js";
