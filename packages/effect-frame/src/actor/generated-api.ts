/**
 * `Generated` as an app reads it: the two generations and the send that
 * fills them. Minting and reading a member's generated fields stays in
 * `generated.ts`, which the view's form imports by path.
 */
export {
  freshId,
  fromCommandId,
  send,
  type Generated,
  type Generation,
  type Input,
  type Member,
  type Mintable,
  type Minted,
} from "./generated.js";
